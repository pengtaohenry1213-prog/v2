# .githooks 目录受保护，请勿修改 pre-commit 脚本

## 管理 Token

Token 文件: `.admin_token`（不在 git 中追踪）

更新密码:
```bash
# 生成随机密码
.githooks/update_token.sh

# 使用指定密码
.githooks/update_token.sh YourNewPassword
```

## 提交 .cursor 目录

```bash
# macOS/Linux
GIT_HOOKS_DIR=.githooks git commit -m "commit message"

# 或直接用脚本验证（会自动读取 .admin_token）
.githooks/pre-commit
```
