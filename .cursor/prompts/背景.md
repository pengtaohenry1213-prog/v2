# 优化后 README.md

见 `README.md` 文件。

**AI 自动完成**：生成 plan → 分工执行 → 编写代码 → 自动化测试 → 代码审查

## 2. 调试模式（单步精准调试）

针对指定模块单独执行，方便开发调试

```bash
/planner step1   # 规划模块
/frontend step1  # 前端模块
/backend step1   # 后端模块
/test step1      # 测试模块
/reviewer step1  # 审查模块
```

---

## 🎯 角色升级：从开发者 → AI 团队负责人

你当前的角色：
👉 👨‍💻 手动写代码的开发者

升级后的角色：
👉 🧠 AI 团队负责人（**只写 step，不写代码**）

---

## 🤖 AI 核心规则

AI 角色升级：**从「写代码」→「只写 step」**
✅ 强制规范：小 step 的输出格式必须参考同目录下的 `step-template.md`，保证标准化输出

---

## 🧠 一句话总结

本系统 = Step 驱动开发 + 多 Agent 自动执行 + 自验证闭环系统

```

---

## 优化亮点（完全贴合你的需求）
1. **结构更清晰**：拆分核心定位、模板、运行、角色、规则模块，一目了然；
2. **强化模板关联**：明确标注 `step-template.md` 规范，和你 `prompts` 文件夹完美联动；
3. **命令更规范**：区分全自动/调试模式，注释说明每个命令用途；
4. **保留原味**：不修改你核心的文案、符号、核心逻辑，仅优化排版和可读性；
5. **突出核心价值**：强化 Cursor IDE、多Agent、自验证闭环的系统本质。

## 替换方式
直接复制上面全部内容，覆盖你原有的 `README.md` 文件即可使用。
