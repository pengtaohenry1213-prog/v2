---
alwaysApply: false
description: 当讨论移动端开发、uni-app、Taro、多端开发、小程序（H5/微信/支付宝）、移动端性能优化、包体体积控制、响应式尺寸适配、条件编译或移动端特定技术问题时应用此规则。
globs:
  - "**/*.vue"
  - "**/*.ts"
  - "**/*.js"
  - "**/pages/**"
  - "**/components/**"
  - "**/src/**"
  - "**/pages.json"
  - "**/manifest.json"
  - "**/app.json"
  - "**/project.config.json"
  - "**/uni.scss"
  - "**/app.scss"
  - "**/app.vue"
  - "**/mini-project/**"
  - "**/wxproject/**"
---
# 移动端（uni-app / Taro / 小程序 / H5）

## 规范约定（引用）

- 全局：`.my-rules/规范/AI_Coding_Style.md`；流程：`rules/SpecCoding.mdc`。
- 与 `rules/frontend.mdc` 一致的 TypeScript 与组件化要求。

## 文件规模（在 AI_Coding_Style 之上略严）

- 页面/逻辑 **Vue/TS 业务代码**：单文件核心逻辑建议 **≤180 行**（不含注释与空行）；配置与工具文件建议 **≤150 行**；超限则拆组件或子模块。

## 分层与目录

- **pages / components / utils / api / store / styles** 等按 uni-app/Taro **当前仓库**为准；**同层目录文件建议 ≤8**。
- 组件 **scoped** 或等价隔离；**组件嵌套建议 ≤3 层**；避免全局样式泄漏。

## 适配

- 尺寸：**rpx / rem** 等以框架与项目约定为准，禁止大段魔法数字 px（设计稿标注除外需转换）。
- **条件编译**仅包裹真实差异，忌复制整页。

## 小程序与包体

- **主包体积**：遵守平台限制（常见 **≤2M**，以微信/支付宝当前要求为准）；超出须分包，首屏相关优先进主包。
- **本地存储**：留意平台单条与总量上限（常见量级单条 **≤1M**、总量 **≤10M**）；敏感数据加密，定期清理无用缓存。
- 图片与无用代码需压缩/清理；存储与隐私按**项目安全约定**处理。

## 性能体验

- 列表与 setData/渲染频率：长列表用虚拟列表或分页等**项目已选方案**。
- 请求：统一封装、错误与登录态处理，忌散落在页面各处。

## 坏味道（与前端共通部分略）

| 症状 | 处理 |
|------|------|
| 单文件巨型页面 | 拆子组件与 composable |
| 尺寸全 px | 改项目约定单位体系 |
| 无错误反馈的网络调用 | 统一拦截与用户提示 |

## 不需要

- 微信/支付宝开发者工具逐步点击教程；ESLint 命令大全。

## 归档（长版）

详见 `.my-rules/Mobile_Development_Engineer.md`。
