---
alwaysApply: false
description: 当讨论数据库设计、SQL 查询、索引优化、数据存储架构、数据库运维、备份恢复、主从复制、缓存策略或数据库故障排查时应用此规则。
globs:
  - "**/*.sql"
  - "**/database/**"
  - "**/db/**"
  - "**/models/**"
  - "**/repositories/**"
  - "**/migrations/**"
  - "**/*.migration.*"
  - "**/config/database*"
  - "**/knexfile*"
  - "**/prisma/**"
  - "**/sequelize/**"
  - "**/typeorm/**"
  - "**/drizzle/**"
---
# DBA / 数据存储

## 定位

**选型、架构、索引与 SQL 习惯、备份恢复、权限**与线上稳定性；变更可回滚、可审计。

## 原则

- 生产**禁止**「无高可用的单点」作为默认形态（具体架构以组织标准为准）。
- **最小权限**账号；生产访问网络受限；敏感字段加密或脱敏策略跟业务与合规文档。
- 变更：优先在线工具或可灰度方案；**有回滚**与验证步骤。

## 设计与查询

- 索引：为真实查询服务；联合索引顺序与**最左匹配**一致；**单表索引数量不宜超过 10 个**（须定期清理无效索引）。
- 分库分表**经验阈值**：单表约 **超 1000 万行** 或单表容量约 **超 50G** 时评估拆分（以监控与业务为准，非硬阈值）。
- SQL：禁止无目的的 `SELECT *`；避免在 WHERE 上对列函数运算导致失效；**批量操作单次体量建议 ≤1000 条**（可按驱动与事务调整）。
- **主从**：主从延迟目标通常 **≤1s**（业务读从库时需评估延迟容忍度）。
- 缓存（如 Redis）：键设计、过期、击穿/雪崩策略与业务文档一致。

## 运维

- 备份策略（全量/增量/保留期）按**组织基线**；恢复演练周期性执行。
- 监控与告警接 Prometheus/Grafana 等**现有体系**，不在规则写安装步骤。

## 故障倾向

| 症状 | 方向 |
|------|------|
| 慢查询突增 | 执行计划、索引、热点 SQL 限流或缓存 |
| 主从延迟大 | 复制与写入形态、硬件、批量事务 |
| 连接打满 | 应用连接池与服务端上限、泄漏排查 |

## 不需要

- pt-toolkit / gh-ost 等工具的完整命令宝典；各数据库版本的全量参数手册。

## 归档（长版）

详见 `.my-rules/DBA.md`。
