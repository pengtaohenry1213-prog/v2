---
description: 前端 UI 开发指南，适用于 Vue3 + Element Plus 项目的界面实现、样式规范、微交互和响应式设计。当任务涉及组件开发、页面布局、CSS 样式、UI 交互或前端视觉优化时激活。
globs:
  - "**/*.vue"
  - "**/*.scss"
  - "**/*.css"
  - "packages/frontend/**/*"
alwaysApply: false
---
# UI（前端视觉与落地）

## 定位

在 **Vue3 + Element Plus**（或项目实际 UI 库）上做出**可交付**界面：层次清晰、可访问性合理、样式可维护；避免模板化「AI 默认审美」。

## 风格基准

- 倾向 **Linear 式**简洁与现代感；**具体色板与字体以设计 token / 主题变量为准**（见 `packages/frontend` 内 theme 或样式变量），本规则只定方向不定死色号。
- 禁用 **Inter / Roboto / Arial / 系统默认** 作为唯一依赖的偷懒组合；选字体时兼顾许可与中文显示。

## 技术落地

- 实现以项目栈为准（如 Vite + Vue + SCSS）；Tailwind 等仅在任务或仓库已采用时使用。
- **深色/浅色**：若产品需要，跟随系统或明确切换策略；实现方式对齐现有代码。
- **大屏/展厅**： vw/vh 或团队已有大屏方案；保证远距离可读（对比度、层级、减少细碎装饰）；动效偏慢、少闪烁；性能上控制 DOM/Canvas 规模。细则与项目「大屏规范」文档互引，避免三处重复维护。

## 微交互

-  hover/focus/禁用态可区分；动效克制，**不**以牺牲性能为代价堆叠动画。

## 质量底线

- 响应式：任务指定的断点需可用；触控目标不宜过小。
- 资源：图片压缩与懒加载按现有工程实践；勿引入未使用的大型依赖。
- **不要**把完整设计系统长篇细则写在本文件；团队若无专文，则在首次任务中落地一版 token 表再引用。

## 归档（历史长版）

更细的超大屏与组件级细则见 `.my-rules/UI.md`（与现行工程栈冲突时以代码与产品为准）。

## 禁止

- 整套复制「反 AI 美学」口号而无具体约束；应落到**可量化的对比度、字号区间、组件状态**等。
- 把 Figma/图标 CDN 教程写进规则（Agent 具备基础能力）。
