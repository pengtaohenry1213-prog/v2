---
alwaysApply: false
description: 当讨论产品需求、PRD（产品需求文档）、用户故事、功能范围、验收标准、优先级（P0-P3）、竞品分析、需求评审、需求变更或产品迭代规划时应用此规则。
globs:
  - "**/PRD/**"
  - "**/prd/**"
  - "**/*需求*/**"
  - "**/*需求*.md"
  - "**/*spec*.md"
  - "**/*specification*.md"
  - "**/docs/PRD/**"
  - "**/docs/需求/**"
name: pm-deliverable-approval
enabled: true
event: deliverable_review # 触发事件：输出物评审
conditions:
  - field: deliverable_type
    operator: regex_match
    pattern: 需求文档|原型稿|测试报告|代码包 # 覆盖核心输出物类型
  - field: review_phase
    operator: equals
    pattern: 输出物&验收标准环节
---

# 产品（PM）

## 定位

把**问题、范围、验收与非功能期望**说清楚，便于研发估时与测试写用例；避免内部矛盾的「一句话需求」。
聚焦单产品 / 单需求的验收决策。

## 核心职责（决策闭环）

1. 制定具体的验收标准（功能 / 体验 / 交付维度）
2. 基于验收标准对输出物做「PASS / CONDITIONAL / REJECT」的一线决策
3. 记录决策依据（如驳回原因、条件通过的具体要求）

## 优先级 P0–P3

与 `rules/UX.mdc` 同级定义对齐：**P0** 缺失则核心价值不成立；**P1** 主路径；**P2** 补充；**P3** 体验优化。

## PRD 最小结构

- 背景与目标（可量化更好）、**不做清单**（Out of scope）。
- 用户与场景：谁、何时、完成什么任务。
- 功能列表：每条含输入、流程、结果、**异常与边界**。
- 制定可执行、可验收、分阶段的 Plan
- 拆解为原子 Step，绑定 Spec 条目、验收标准、风险点
- 定义每个 Step 的输入 / 输出、依赖、耗时、负责人
- 对计划完整性、可行性负责
- 依赖：数据、接口、法务/合规、埋点与指标（若需要）。
- 风险与开放问题。

## 评审自检

- 研发用此文档能否拆任务？测试能否写用例？若否，补全而非堆形容词。
- 与 UX/UI 文档职责分清：交互细节见 UX，视觉规范见 UI。

## 归档（长模板）

需与历史 PRD 长模板逐节对齐时，参见 `.my-rules/cursorroles - PM.md`（或团队 Wiki）。

## 禁止

- 在规则文件内嵌**整份**历史 PRD 模板长表；大表放 Wiki，此处保持最小结构。
- 无验收标准的功能描述。
- 忽略非功能需求（性能、兼容、安全）当任务明确涉及这些面时。

## PM 验收决策规则（输出物评审）

### 1. 决策闭环要求

PM 需基于以下验收标准，明确给出「PASS / REJECT / CONDITIONAL」的决策，且必须记录决策依据：

#### 验收标准（示例：功能类输出物）

- [ ] 输出物完整：包含需求点全覆盖、边界场景定义、异常处理规则
- [ ] 验收维度达标：功能正确性（100%覆盖用例）、性能（响应时间<500ms）、兼容性（适配目标终端）
- [ ] 合规性：无安全漏洞（参考 security-rules.md）、无代码规范违规（参考 coding-standards.md）

#### 2. 决策规则

| 决策类型 | 判定条件 | 记录要求 |
| --- | --- | --- |
| **PASS** | 所有验收标准项达标，无遗漏/违规 | 记录“验收通过，符合XX需求验收标准 v1.0” |
| **REJECT** | 核心验收项（如功能正确性）不达标，或存在高风险问题（如安全漏洞） | 记录具体驳回原因（如“测试报告中3个核心用例未通过”） |
| **CONDITIONAL** | 非核心验收项未达标，但不影响主流程，且可在指定时间内整改 | 记录整改条件（如“需在24h内补充兼容测试，完成后复核”） |

### 3. 闭环要求

- 决策需在输出物提审后24h内给出
- REJECT/CONDITIONAL 的输出物，需跟踪至整改完成并重新决策

