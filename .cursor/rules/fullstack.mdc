---
alwaysApply: false
description: 当讨论架构设计、技术选型、模块拆分、前后端分层、性能优化、技术债务、系统边界权衡，或需要进行跨端（前端/后端/数据库）方案评审时应用此规则。
globs:
  - "**/packages/**"
  - "**/src/**"
  - "**/*.ts"
  - "**/*.tsx"
  - "**/*.js"
  - "**/*.jsx"
  - "**/vite.config.*"
  - "**/tsconfig*.json"
  - "**/express/**"
  - "**/router/**"
  - "**/service/**"
  - "**/api/**"
---

# 全栈 / 架构视角（按需启用）

在对话中需要架构权衡、拆分或跨端方案时启用本规则；日常小改动不必默认加载。

## 定位

对标业务现状做**可落地的**前后端一体化设计：边界清晰、可演进、安全与可观测有默认项，避免为「未来假想规模」过度设计。

## 本仓库上下文（与实现对齐）

- Monorepo（pnpm）；当前可见主力为 `packages/frontend`（Vue3 + Vite 等以 `package.json` 为准）。
- 后端若未在本仓：接口契约、错误码、`code/msg/data` 等与后端仓库或接口文档**对齐**，勿在各规则文件重复全文。
- 全局编码与文件组织：`.my-rules/规范/AI_Coding_Style.md`。

## 决策检查（简）

- **业务**：是否解决明确问题？能否拆成可交付增量？
- **分层**：前端（页面 / 组件 / 状态 / 请求）；后端（路由 / 服务 / 数据访问）。禁止业务逻辑堆在路由或组件巨型脚本中。
- **质量**：鉴权、输入校验、密钥进环境变量、关键路径可观测；性能按真实瓶颈优化，先测量再改。
- **性能**：第一时间优先考虑：空间复杂度、大数据量、性能最优这些点

## 常见反模式（见症下药）

| 症状 | 方向 |
| --- | --- |
| 抽象层从未被第二张表/第二个调用方使用 | 删减，YAGNI |
| 模块间直连穿透多层 | 收敛 API，考虑事件/消息仅在高扇出时引入 |
| 只加功能不加测试/监控 | 为核心路径补最小测试或观测 |

## 工具分工边界

**判定流程**（满足任一条件 → Claude Code；均不满足 → Cursor Agent）：

```mermaid
flowchart TD
    A[任务开始] --> B{涉及环境变量 / 容器 / 数据库 / 配置文件?}
    B -- 是 --> Z[Claude Code]
    B -- 否 --> C{跨目录 / 跨包批量修改?}
    C -- 是 --> Z
    C -- 否 --> D{是单文件 / 单组件内逻辑?}
    D -- 是 --> Y[Cursor Agent]
    D -- 否 --> E{涉及 3+ 文件但结构清晰?}
    E -- 是 --> F["拆分：Cursor 实现 + fullstack.mdc 架构评审"]
    E -- 否 --> Z

当 Cursor Agent 判定任务属于 Claude Code 范畴时，输出提示：
→ 此任务建议切换 Claude Code 处理，Cursor Agent 继续微观开发
```

### 宏观任务 → Claude Code（终端运行）

| 任务类型 | 判断条件 |
|----------|----------|
| 新项目目录结构初始化 | pnpm init、目录分层、基础配置文件生成 |
| 跨目录 / 跨包批量修改 | 涉及 `apps/`、`packages/`、`database/` 等跨目录改动，或修改点超过 2 个包 |
| 涉及配置文件 / 环境变量 | `.env`、`docker-compose`、`nginx.conf`、`tsconfig.json` 等全局配置修改 |
| 环境 / 容器 / 数据库排障 | Docker 启动失败、MySQL/Redis 报错日志分析、修复命令 |
| RAG / AI 应用整体架构 | 知识库分层、向量库选型、召回策略、部署方案 |
| 毕设 / 技术文档撰写 | 基于项目代码输出规范的技术章节 |

### 微观任务 → Cursor Agent（IDE 内运行）

| 任务类型 | 说明 |
| --- | --- |
| 单文件组件 / 函数 / 接口编写 | 当前文件上下文内的业务逻辑补全 |
| 单页面样式、交互、逻辑 | Vue/React 组件、小程序页面、局部 UI 调试 |
| 小范围 bug 定位和修复 | 单函数 / 单模块的错误分析，最小改动修复 |
| 代码语法优化和格式调整 | ESLint / Prettier 可处理的风格问题 |
| 实时光标上下文内补全 | Cursor 的 Cmd+K / Tab 补全覆盖的场景 |

### 协作关系

- 本规则（`fullstack.mdc`）≈ `prompts/planner.mdc` 的 Plan 生成阶段：都在做宏观架构层面的工作
- `planner.mdc` 产出结构化 Plan（`.cursor/plans/stepN-plan.md`），本规则负责其中的架构评审和质量把关
- 灰色地带（3-6 文件但结构清晰）：拆分处理 — Cursor 实现具体文件 + fullstack.mdc 架构评审
- 边界不明确时：主动询问用户，由用户决定交给哪个工具

## 延伸阅读（引用）

- 流程红线：`rules/SpecCoding.mdc`
- 前端实现约束：`rules/frontend.mdc`
- Node/Express 约束：`rules/backend.mdc`
- 历史长版叙事：`.my-rules/fullstack.md`
