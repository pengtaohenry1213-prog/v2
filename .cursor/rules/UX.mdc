---
description: 当涉及用户流程设计、交互细节、空状态/错误状态处理、可用性结论、大屏交互、IXD 文档输出或用户体验优化时应用此规则。
globs:
  - "**/*ux*.md"
  - "**/*interaction*.md"
  - "**/*flow*.md"
  - "**/ixd/**"
  - "**/UX/**"
  - "**/interaction/**"
alwaysApply: false
---

# UX / 交互

## 定位

用户流程、关键交互、状态与异常、与研发的实现要点；产出可对接 UI 与前端，避免仅形容词堆砌。

## 优先级 P0–P3

- **P0**：完不成则核心任务无法达成。
- **P1**：主路径体验或效率明显受损。
- **P2**：支线或次要场景。
- **P3**：优化与抛光。

## 交付最小结构（按需扩展）

1. **背景与目标**：用户是谁、要解决什么问题、如何验收。
2. **主路径 + 分支 + 异常**：可用简短步骤或流程图链接；异常须含系统/网络/权限等。
3. **关键交互表**（示例列）：页面或模块 | 触发 | 反馈 | 空/错/加载态。
4. **研发对接**：数据从哪来、权限、埋点是否需要、与现有组件的差异。

完整 IXD 大模板若团队有 Wiki/专文，**引用路径即可**，不必每次输出超长固定表头。

## 可用性与大屏

- 若涉及**可用性结论**：说明方法、样本量、结论与下一轮验证项，避免无依据断言。
- 大屏/远距阅读：触控目标与字号、对比度、动效时长等**写可操作数值或引用设计规范**，由 UI 规则（`rules/UI.mdc`）配合视觉。

## 符号（若需统一状态标记）

沿用团队约定：`✅` `❌` `🔴` `🟡`；无刚需可仅用文字状态。

## 归档（长模板）

需与历史 IXD 长模板逐节对齐时，参见 `.my-rules/UX.md`（或团队 Wiki）。

## 禁止

- 与 PRD/视觉稿矛盾的交互描述。
- 遗漏主路径异常与空状态。
- 为凑模板填「无意义占位」——无内容则明确写「未覆盖 / 待产品补充」。
